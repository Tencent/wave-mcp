# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
*.so

# venv / runtime
.venv/
venv/
runtime/

# IDE / tooling
.codebuddy/
.idea/
.vscode/
.pytest_cache/

# stray local artifacts (keep examples/sample/*.fst|*.vcd as quickstart data)
/test.fst.hier

# offline bundle build outputs (built via deploy/, not tracked)
wave-mcp-bundle*/
*.tar.gz

# internal test harnesses & reports (OpenTitan/XiangShan third-party code and
# large test data; not published). Keep: unit/, README, regression entry.
tests/projects/
tests/reports/
tests/archive/
tests/fourstate/sim/
tests/fourstate/session/
tests/fourstate/session_ext/
tests/functional_verify.py
tests/full_quality_check_all_tools.py

# fieldkit run artifacts (the tool itself IS published)
fieldkit_session/
fieldkit_report.json

.agent

# uploaded files (temporary)
knot-uploads/

# internal dev docs (roadmaps, plans, todos — not for public release)
dev-docs/
