# Core MCP framework (SDK v2, protocol 2026-07-28; v2 includes the CVE-2026-52869/52870 fixes)
mcp>=2.0.0,<3

# FST waveform reader (C fstapi bindings, random-access, fast)
pylibfst>=0.2.1

# SystemVerilog elaboration for connectivity / driver / trace (categories 5/6).
# Full elaboration, ships as a manylinux wheel (glibc >= 2.27), pip-installable
# and portable to the air-gapped network. Single static-analysis backend.
pyslang>=11.0.0

# System binary (not a pip dep): vcd2fst (GTKWave) for VCD->FST conversion.
# Bundled in the offline runtime package for air-gapped deployment.
